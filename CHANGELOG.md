# Changelog

## Unreleased

### Repository

- Added Phase 4.7 repository cutover with root-native Composer, PHPUnit, PHPStan, PHP-CS-Fixer, Deptrac, release-validation, package split validation, supply-chain checks and developer-experience commands; removed the tracked workspace directory and EvolvePHP 1 runtime files from the 2.x working tree while preserving master and Git history. The PHP 8.4/8.5 root quality matrix now runs from the repository root; the protected `Workspace quality (PHP 8.4)` and `Workspace quality (PHP 8.5)` context names are retained for governance compatibility.

### Component foundation

- Added Phase 5.4 restricted component service-definition registration and definition contribution, including public pre-beta experimental `ServiceDefinitionRegistrar` in `evolvephp/contracts`, direct Contracts PSR-11 factory-contract metadata, Core-owned exact `ComponentGraphDeclaration` binding, `ResolvedComponentGraph` ordering, staged Application/Execution/Transient service definitions, reserved existing service identifiers, deterministic duplicate/collision attribution, atomic publication into `ServiceRegistry`, failed registration publishes nothing from the staged batch, numeric-looking service-ID hardening and no service resolution, no service construction and no automatic registry freeze. `ApplicationKernel` integration, entry-point lifecycle and boot remain Phase 5.5; discovery and enablement remain Phase 5.6.
- Added Phase 5.3B Core-owned dependency/capability graph validation and deterministic resolution in `evolvephp/core`, including public pre-beta experimental `ComponentGraphResolver`, `ResolvedComponentGraph`, consumer-scoped `CapabilityProviderSelection`, typed graph-resolution failures, duplicate active identifier detection, required and present-optional dependency ordering, active conflict validation, `ExactlyOne` and `OneOrMore` provider resolution, effective-edge deduplication, deterministic dependency-first ordering and canonical dependency-cycle diagnostics while consuming the accepted Contracts graph declarations only; registration, boot, ready, shutdown, discovery, enablement, component versions, dependency version ranges and Composer semantic-version constraint evaluation remain deferred.
- Added Phase 5.3A dependency/capability graph declaration vocabulary in `evolvephp/contracts`, including public pre-beta experimental `ComponentGraphDeclaration`, `ComponentGraphRelations`, required/optional dependency declarations, declarative conflicts, capability identifiers, required capability cardinality with `ExactlyOne` and `OneOrMore`, provided capability identifiers, canonical relation ordering with no startup-order semantics and descriptor `graphDeclaration()` projections in `evolvephp/module` and `evolvephp/plugin` while keeping existing three-argument descriptor construction valid; graph validation/resolution, provider selection, cycle detection, startup ordering, component versions, version constraints, SemVer graph decisions, registration, lifecycle, discovery and Core integration remain deferred to Phase 5.3B or later.
- Added Phase 5.2 immutable descriptor foundation in `evolvephp/module` and `evolvephp/plugin`, including public pre-beta experimental `ModuleDescriptor`, `PluginDescriptor`, package-owned EvolvePHP-major compatibility validation and typed compatibility failures; component versions, dependencies, capabilities, registration, entry-point lifecycle, boot, discovery, enablement, serialization and Core integration remain deferred.
- Added Phase 5.1 Component Identity Foundation in `evolvephp/contracts`, including public pre-beta experimental `ComponentIdentifier` and `ComponentType` vocabulary for future Module and Plugin metadata; Module/Plugin entry points, descriptors, discovery, enablement, dependency/capability graphs, registration, boot/ready/shutdown orchestration and Core integration remain deferred.

### Runtime foundation

- Added Phase 4.6 runtime-neutral `ResponseEmitter` boundary and Phase 4 integration acceptance in `evolvephp/http`, including an explicit post-resolution emission boundary after `ExecutionOutcomeResponseResolver`, integration coverage across accepted Phase 4 middleware, routing, execution, response/error and health components, and no concrete runtime/SAPI emission or OpenTelemetry propagation.
- Added Phase 4.5 HTTP response/error and health foundation in `evolvephp/http`, including `ExecutionOutcomeResponseResolver` post-`HttpKernel` mapping for exact successful responses, empty 404/405/500 framework responses with exact 405 `Allow` metadata, minimal `ReadinessCheck`, explicit bodyless `LivenessHandler` and `ReadinessHandler`, and no concrete PSR-7 implementation, response emission, automatic health routes, runtime adapters, logging, metrics or OpenTelemetry.
- Added Phase 4.4 HTTP execution-kernel integration in `evolvephp/http`, including public `HttpKernel` delegation to Core `ExecutionOrchestrator` as `ExecutionKind::HttpRequest`, authoritative request-local `ExecutionContext::class` and `ExecutionScope::class` attributes, `ExecutionOutcome` return semantics, primary response/throwable preservation, cleanup/reset failure separation, quarantine reuse decisions and generic Core instrumentation reuse; PSR-17, 404/405 responses, `Allow` headers, health endpoints, emitters, SAPI integration, process recycle adapters, OpenTelemetry, Insight and Observe remain deferred.
- Added Phase 4.3 routed handler dispatch in `evolvephp/http`, including public `RoutingRequestHandler`, authoritative `RouteMatch::class` request attributes, ordered post-match middleware composition and typed `RouteNotFound` / `MethodNotAllowed` routing failure boundaries with exact allowed-method preservation; 404/405 responses, `Allow` headers, PSR-17 response factories, HTTP kernel integration, health endpoints, trace propagation, OpenTelemetry, Insight and Observe remain deferred.
- Added Phase 4.2 routing foundation in `evolvephp/http`, including immutable route definitions, ordered `RouteCollection`, exact method/path matching through `RouteMatcher`, whole-segment `{parameter}` templates, raw non-decoded parameter capture, insertion-order match precedence, method-mismatch continuation, `allowedMethods()` metadata and handler storage without handler invocation; routed dispatch, 404/405 responses, route middleware composition, response factories, HTTP kernel integration, health endpoints, trace propagation, OpenTelemetry, Insight and Observe remain deferred.
- Added Phase 4.1 PSR HTTP interoperability and deterministic middleware pipeline foundation in `evolvephp/http`, including PSR-7 message interface consumption, PSR-15 middleware/handler interface consumption, public `MiddlewarePipeline`, internal immutable-position dispatcher state, middleware order preservation, short-circuiting, replacement-request forwarding and unchanged throwable propagation; routing, HTTP execution-kernel integration, response factories, concrete PSR-7 implementation, response emission, SAPI integration, health endpoints, trace propagation, OpenTelemetry, Insight and Observe remain deferred.
- Added Phase 3.7 minimal console foundation with a runtime-neutral `Command` contract, raw ordered `CommandInput` tokens, runtime-neutral `CommandOutput`, exit-code-only `CommandResult`, an immutable deterministic `CommandRegistry`, `CommandRunner` integration with `ExecutionOrchestrator`, `ExecutionKind::CliCommand` classification and an unknown-command dispatch boundary before execution starts; shell executables, argv parsing, option parsing, STDIN/STDOUT/STDERR integration, Symfony Console, Doctor, generators and production CLI runtime adapters remain deferred.
- Added Phase 3.6 generic execution observations for the Core `ExecutionOrchestrator`, including a safe immutable observation model, constrained observation types and outcomes, an optional void observation sink, dispatcher-level sink failure isolation, separate instrumentation-failure reporting on `ExecutionOutcome`, deterministic observation ordering and unchanged primary, cleanup and reusable/quarantine semantics; Insight, Observe, OpenTelemetry support, tracing, metrics, logs, production telemetry, storage, exporters, HTTP instrumentation, module/plugin instrumentation, Bridge instrumentation and runtime-adapter instrumentation remain deferred.
- Added Phase 3.5 runtime-neutral execution orchestration with Core-owned execution identifiers, execution-kind classification, immutable execution context, exactly-once operation invocation, finally-equivalent execution-scope cleanup, terminal outcome reporting, primary handler success/failure preservation, separate cleanup/reset failure reporting, explicit reusable/quarantine decisions and orchestrator-local refusal after quarantine; HTTP, queue, scheduled-job and CLI adapters, retry policy, process recycling, telemetry, module/plugin runtime, streaming and concurrency remain deferred.
- Added Phase 3.4 execution-scope and reset foundation with frozen Execution definitions, explicit post-freeze scope creation, root PSR-11 resolver preservation, root `has()`/`get()` Execution separation, one Execution value per scope, Application identity shared across scopes, Transient non-caching, resolver-context captive-dependency prevention, explicit `ResetParticipant` registration, reverse registration reset ordering, terminal/idempotent close, aggregate reset failures and execution-local reference release; execution identifiers/context, orchestration, outcomes, quarantine, HTTP, module/plugin runtime, console, telemetry and concurrency claims remain deferred.
- Added Phase 3.3 service-container foundation with explicit Core service registration, an idempotent freeze boundary, PSR-11 read-only `has()`/`get()` interoperability, application and transient lifetimes, initial Execution lifetime vocabulary, deterministic unknown-service, circular-dependency and factory-failure behavior, and `ApplicationKernel` freeze-before-readiness integration; autowiring, aliases, service tags, decorators, service-locator globals, HTTP, module/plugin runtime, console and telemetry remained deferred to later slices.
- Added Phase 3.2 configuration foundation with read-only public configuration and validation contracts, a public configuration exception catch boundary, immutable Core array-backed configuration, scalar/null/recursive-array value validation, dot-path lookup with distinct null and missing semantics, deterministic boot-time validator execution, fail-fast validation behavior, terminal failed-kernel instances, and internal Booting/Failed lifecycle states; environment loading, dotenv, configuration files, HTTP, module/plugin runtime, console and telemetry remain deferred, while container/PSR-11 work was left to the later Phase 3.3 slice.
- Added Phase 3.1 application lifecycle foundation with a narrow public boot/shutdown contract, lifecycle exception catch boundaries, and a minimal Core `ApplicationKernel` state transition implementation; complete kernel behavior, container integration, execution handling, reset behavior, HTTP, module/plugin runtime, console and telemetry remain deferred.

### Documentation and governance

- Added Phase 2.10B deterministic package split and prerelease consumer validation with repository-owned `git subtree` split checks, exact tree/inventory equivalence, generated split Composer validation, an offline alpha/stable consumer matrix, documented alpha consumer policy, retained internal `^2.0` constraints, Policy PHP 8.4 split-validation enforcement, manual/pre-release consumer validation, and no remote writes, tags or release artifacts.
- Added Phase 2.10A deterministic package release validation with an explicit release package map/order, package-local README/licence preparation, and no remote publication or splitting yet.
- Added Phase 2.9A supply-chain security foundation with Composer lockfile security audit enforcement, abandoned-package failure, locked production and development licence-policy checks for MIT, BSD-3-Clause and Apache-2.0, repository-owned Dependabot version-update configuration, Policy job enforcement, and documentation of GitHub setting boundaries.
- Added Phase 2.8 developer-experience foundation with EditorConfig, VS Code extension recommendations, portable VS Code settings and portable task commands, PHP 8.4 language-analysis targeting, canonical Composer-script reuse, explicit non-mutating quality checks versus the Style Fix mutating task, no local executable paths, and no runtime debugging configuration.
- Added Phase 2.7B repository governance evidence finalization: changed the GitHub default branch to `2.x`, activated repository rulesets for `master` and `2.x`, preserved `master` as the EvolvePHP 1 legacy line, required PR-based change on both branches, blocked deletion and force pushes, enforced strict/up-to-date required status checks on `2.x` for `Policy (PHP 8.4)`, `Workspace quality (PHP 8.4)` and `Workspace quality (PHP 8.5)`, and made no branch rename or deletion.
- Added Phase 2.7A repository-owned branch-governance policy foundation for EvolvePHP 2 development on `2.x`, preserved EvolvePHP 1 maintenance on `master`, explicit separation of modern development and legacy maintenance, and preparation for a later default-branch and ruleset transition.
- Added Phase 2.6 GitHub Actions CI foundation with a PHP 8.4/8.5 workspace quality matrix, separate PHP 8.4 root-policy job, lockfile-based workspace installation, immutable action pinning, and successful initial CI execution for the current workspace, tooling and package foundation.
- Added Phase 2.5.1 README and metadata consistency cleanup, aligning the EvolvePHP 2 README hierarchy, correcting root Composer metadata, deduplicating stale RFC index narration, removing duplicated phase history, and clarifying workspace installation and compatibility guidance.
- Added Phase 2.5 workspace-owned Deptrac architecture and dependency-boundary enforcement for the initial EvolvePHP 2 package graph.
- Added Phase 2.4 workspace-owned PHPStan level 6 static analysis, PHPUnit type-inference integration, PHP-CS-Fixer PER Coding Style 3.0 checks, and architecture-policy tests for cache, baseline and tooling ownership.
- Added Phase 2.3 PHPUnit 13 workspace foundation, six package test suites, and the initial workspace lockfile generated under PHP 8.4.
- Added Phase 2.2 dedicated EvolvePHP 2 Composer workspace with local path-repository integration and explicit 2.0.x-dev package version mapping.
- Added Phase 2.1 initial EvolvePHP 2 package skeleton boundaries for the accepted alpha package map.
- Added Cross-RFC terminology harmonization, Bridge dependency-direction clarification, execution-scoped tenant-context clarification, and telemetry finalization and scope-closure ordering clarification for accepted EvolvePHP 2 RFCs 0001-0006.
- Added `AGENTS.md` as the mandatory workflow rule source for coding agents working in this repository.
- Added EvolvePHP 1 legacy preservation documentation under `docs/history/`.
- Added RFC 0001: EvolvePHP 2 Vision, Scope and Non-Goals.
- Added RFC 0002: Terminology, Package Boundaries and Public Contracts.
- Added RFC 0003: PHP Versioning, Compatibility and Release Policy.
- Added RFC 0004: Module and Plugin Lifecycle.
- Added RFC 0005: Request Scope, Runtime Reset and Persistent-Worker Safety.
- Added RFC 0006: Evolve Bridge and Incremental Modernisation.
- Added RFC 0007: Insight and OpenTelemetry Architecture.
- Added `SECURITY.md` with private vulnerability-reporting guidance and supported-line status.
- Added `SUPPORT.md` with usage, bug-report and security-report guidance.
- Added documentation-policy tests for the Phase 0 preservation baseline.

### Legacy baseline

- Preserved the audited EvolvePHP 1 baseline at commit `2da5da7866f65d314a0e2bf10b572004b3014d60`.
- No `1.0.0-legacy` Git tag was found during the Phase 0 audit.
- No legacy runtime modernization, dependency upgrade, PHP requirement change or namespace rename is included in this baseline documentation work.
